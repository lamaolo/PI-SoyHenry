const initialState = {
  countries: [],
  countryDetail: {},
};

const reducers = (state = initialState, action) => {
  switch (action.type) {
    default: {
      return state;
    }
  }
};

export default reducers;
